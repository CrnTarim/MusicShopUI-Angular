import { SingerSearchPipe } from './singer-search.pipe';

describe('SingerSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new SingerSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
